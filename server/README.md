# Pharma Luz AI Backend (minimal scaffold)

This is a minimal Node/Express backend that exposes a simple `/chat` endpoint.

Install and run:

```bash
cd server
npm install
npm start
```

The endpoint:
- POST /chat  { "message": "..." } -> { "reply": "..." }

This is a scaffold for development — replace with a real AI integration when ready.
